[sound]

// the maxumium amount of concurent sounds
max_sounds = 3

// minimum interval in milliseconds before the next sound is allowed
min_interval = 50

// minimum and maximum distance
min_dist = 15
max_dist = 180

// sound volume (percentage), applies to 2D sounds only!
// volume = 100

// chance to play a sound at all for the current folder
// chance_to_play = 100

// disable cache for the current folder
//cached = 0
